// Elements that should always be present in the palette
export const DEFAULT_ELEMENTS = ['Receiver', 'Exit']

// Elements that should never be present in the palette
export const NON_CANVAS_ELEMENTS = ['Forward']
